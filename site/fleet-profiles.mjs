// Public architecture at the linked source revision; these are diagrams, not live telemetry.
const source = (path, line) => `https://github.com/h3ro-dev/borg/blob/29e5977b2d463425f33dd91e131cfdc6279bac58/${path}#L${line}`;
const memoryModels = {
  key: 'models', anatomy: 'Model & adapter bay', name: 'Local models. Optional adapters.',
  status: 'FULL INSTALL / ADAPTERS INACTIVE',
  description: 'Qwen3 4B extracts facts and relationships. Nomic Embed Text turns text into 768-dimensional vectors. Three supplied MLX LoRA adapters target Qwen3 1.7B and 4B extraction; all start inactive and need preparation and validation before activation.',
  stack: ['Ollama', 'qwen3:4b', 'nomic-embed-text', '3 inactive MLX LoRAs'],
  source: source('adapters/README.md', 36),
};

export const profiles = [
  {
    id: 'memory', number: '01', role: 'THE COLLECTIVE / MEMORY & CONTEXT',
    name: 'The memory core.', mark: './assets/mark.svg', setup: './guide.html#understand',
    summary: 'Facts become recall. Events become relationships. This is the shared memory anatomy that lets connected agents carry useful context from one task to the next.',
    tags: ['Mem0', 'Graphiti', 'Qdrant', 'FalkorDB'],
    parts: [
      { key: 'cortex', anatomy: 'Recall cortex', name: 'Mem0 + Qdrant', status: 'FULL INSTALL',
        description: 'Mem0 manages scoped facts and their history. Qdrant stores their embeddings so an agent can retrieve relevant context by meaning, not just by matching words. A new owner starts with empty stores.',
        stack: ['Scoped facts', '768D semantic vectors', 'SQLite history'], source: source('memory/bin/mem0ctl', 606) },
      { key: 'core', anatomy: 'Extraction core', name: 'Qwen reads. Nomic embeds.', status: 'LOCAL MODEL SERVICES',
        description: 'Ollama runs the local memory models. Qwen3 4B helps extract supported facts and graph relationships; Nomic Embed Text produces vectors. These jobs are separate from the GPT, Claude or Grok model doing your main task.',
        stack: ['Ollama', 'qwen3:4b', 'nomic-embed-text:latest'], source: source('installer/config.py', 192) },
      { key: 'spine', anatomy: 'Relationship spine', name: 'Graphiti + FalkorDB', status: 'BACKGROUND GRAPH FLOW',
        description: 'Graphiti turns episodes into entities and time-aware relationships in FalkorDB. A background projector makes graph context available for recall. Graph updates run asynchronously, so a new fact is not instantly a finished graph.',
        stack: ['Entities', 'Relationships over time', 'Graph recall projector'], source: source('graph/bin/graph-recall-projector', 434) },
      { key: 'tools', anatomy: 'Context interface', name: 'Recall enters the agent.', status: 'CONNECTED CLIENTS',
        description: 'BORG exposes memory search, explicit remember and project context through its tools. Native Codex can also receive bounded automatic recall. Returned memories are candidates that an agent checks against current sources.',
        stack: ['Search', 'Remember', 'Project context', 'Source verification'], source: source('connector/README.md', 7) },
      memoryModels,
    ],
    flow: [
      { label: 'Supported facts', part: 'core', detail: 'An explicit remember call or a configured capture hook supplies scoped, supported facts.' },
      { label: 'Embed & store', part: 'cortex', detail: 'Nomic generates embeddings. Mem0 stores the accepted facts, with semantic vectors in Qdrant.' },
      { label: 'Build relationships', part: 'spine', detail: 'The background graph feed sends episodes to Graphiti, which records entities and temporal relationships in FalkorDB.' },
      { label: 'Project context', part: 'spine', detail: 'The graph recall projector reuses existing fact embeddings to make graph-derived context searchable.' },
      { label: 'Scoped recall', part: 'cortex', detail: 'A query retrieves relevant candidates within the owner and project scope.' },
      { label: 'Verify & use', part: 'tools', detail: 'The agent checks recalled information against the current repository, tools or other original sources before acting.' },
    ],
    boundaries: ['The full install includes the memory stack. The tools-only profile skips memory, graph services, model pulls and memory hooks.', 'Your stores start empty. Training-pair capture is a separate opt-in; the bundled adapters are inactive by default.'],
  },
  {
    id: 'codex', number: '02', role: 'OPENAI / GPT & CODEX', name: 'The context-aware builder.',
    mark: './assets/providers/openai.svg', setup: './guide.html#providers',
    summary: 'Follow a task through the native Codex memory loop: selective automatic retrieval before work, tools during execution, and validated fact capture afterward.',
    tags: ['Native Codex hooks', 'Automatic recall', 'Automatic fact embedding'],
    parts: [
      { key: 'cortex', anatomy: 'Recall cortex', name: 'Automatic, selective retrieval.', status: 'TRUSTED CODEX HOOKS',
        description: 'On a meaningful topic or project change, the prompt hook embeds a recall query and retrieves ranked context. It injects a small, deduplicated selection into Codex: up to four items and roughly 700 tokens, with a per-generation injection limit.',
        stack: ['UserPromptSubmit', 'Mem0 search', 'Bounded additionalContext'], source: source('memory/bin/mem0-codex-hook', 832) },
      { key: 'core', anatomy: 'Provider core', name: 'Your Codex identity.', status: 'OWNER SIGN-IN REQUIRED',
        description: 'The OpenAI insignia marks the reasoning agent. Your authenticated native Codex client performs the task; the conductor can route native work using its configured accounts, capacity and work claims. The installer does not choose or pin your reasoning model.',
        stack: ['Native Codex', 'Owner authentication', 'Conductor routing'], source: source('docs/SETUP.md', 100) },
      { key: 'spine', anatomy: 'Capture spine', name: 'Facts back into memory.', status: 'STOP / SESSION END',
        description: 'Detached capture selects redacted user and tool evidence, extracts facts with local Qwen, validates them and skips duplicates. Accepted facts are embedded and stored. Assistant claims and entire raw transcripts are not automatically treated as memory.',
        stack: ['Evidence selection', 'Qwen extraction', 'Validation', 'Nomic embeddings'], source: source('memory/bin/mem0-codex-hook', 1530) },
      { key: 'tools', anatomy: 'Tool arm', name: 'From context to execution.', status: 'MCP CONNECTION',
        description: 'The native MCP connection exposes the BORG memory and computer tools. Files, commands, browser sessions and registered remote targets remain real tool calls with their own permissions and operation receipts.',
        stack: ['Memory', 'Files & processes', 'Browser', 'Remote targets'], source: source('installer/clients.py', 58) },
      memoryModels,
    ],
    flow: [
      { label: 'Prime session', part: 'core', detail: 'SessionStart prepares the memory generation. Starting a session does not itself embed the conversation.' },
      { label: 'Embed query', part: 'cortex', detail: 'When the refresh gate detects a meaningful topic or project change, the hook embeds a recall query.' },
      { label: 'Retrieve context', part: 'cortex', detail: 'Ranked, bounded and deduplicated memory candidates enter Codex as additional context.' },
      { label: 'Reason & execute', part: 'tools', detail: 'Your Codex model works with the recalled context and calls BORG tools as needed.' },
      { label: 'Extract facts', part: 'spine', detail: 'At Stop or SessionEnd, detached capture redacts and examines supported user and tool evidence.' },
      { label: 'Embed facts', part: 'models', detail: 'Validated new facts receive Nomic embeddings and enter Mem0; background graph processing can follow.' },
    ],
    boundaries: ['This automatic loop belongs to the trusted native Codex hooks in a full installation.', 'ChatGPT in the browser connects separately through the optional OAuth MCP gateway. It can explicitly search and remember; it does not inherit native Codex hooks.'],
  },
  {
    id: 'claude', number: '03', role: 'ANTHROPIC / CLAUDE CODE', name: 'The connected collaborator.',
    mark: './assets/providers/claude.svg', setup: './guide.html#providers',
    summary: 'Connect Claude Code to the same memory and tools, using your own authenticated profile. Inspect where explicit recall, optional hooks and tool execution meet.',
    tags: ['Claude Code', 'Owner-configured MCP', 'Optional launch bus'],
    parts: [
      { key: 'cortex', anatomy: 'Recall cortex', name: 'Ask for relevant context.', status: 'EXPLICIT MEMORY TOOLS',
        description: 'After you connect the BORG MCP server, Claude can search scoped memories and project context. The shared store supplies candidates; Claude checks their relevance against the current task.',
        stack: ['Mem0 search', 'Project context', 'Owner scopes'], source: source('docs/SETUP.md', 116) },
      { key: 'core', anatomy: 'Provider core', name: 'Your Claude Code profile.', status: 'MANUAL SETUP',
        description: 'Install Claude Code, sign in and register BORG with your own profile. An optional headless launch bus can invoke claude -p. It starts disabled and does not forward a requested packet model; choose the model in your Claude configuration.',
        stack: ['Claude Code', 'CLAUDE_CONFIG_DIR', 'Optional headless bus'], source: source('conductor/providers/launch-bus.mjs', 21) },
      { key: 'spine', anatomy: 'Memory spine', name: 'Explicit writes. Optional hooks.', status: 'HOOKS REQUIRE SETUP',
        description: 'Claude can explicitly remember supported facts through MCP. The repository also includes Claude recall and capture hook source, but registering MCP alone does not install, trust or activate those hooks.',
        stack: ['Explicit remember', 'mem0-claude-hook', 'mem0-capture-hook'], source: source('memory/bin/mem0-claude-hook', 2) },
      { key: 'tools', anatomy: 'Tool arm', name: 'A shared computer interface.', status: 'AFTER MCP CONNECTION',
        description: 'Connected Claude sessions can use BORG file, process, browser and fleet tools within the owner’s configuration and permissions. The native client remains responsible for approving and running its tool calls.',
        stack: ['Files & commands', 'Browser sessions', 'Fleet targets'], source: source('connector/README.md', 7) },
      memoryModels,
    ],
    flow: [
      { label: 'Install & sign in', part: 'core', detail: 'The owner installs and authenticates Claude Code with their own provider account.' },
      { label: 'Connect BORG', part: 'tools', detail: 'Register the BORG stdio MCP server in the chosen Claude profile.' },
      { label: 'Request context', part: 'cortex', detail: 'Claude explicitly searches the shared memory or project context through tools.' },
      { label: 'Perform work', part: 'tools', detail: 'The native client, or a separately configured headless bus, performs the task using its available tools.' },
      { label: 'Remember facts', part: 'spine', detail: 'Use explicit remember calls, or separately configured capture hooks, to persist supported new information.' },
      { label: 'Verify outcome', part: 'core', detail: 'Check the actual files, tests and service results. A launch receipt alone does not prove completed work.' },
    ],
    boundaries: ['Claude setup and authentication belong to the owner. MCP registration does not enable automatic memory hooks.', 'The optional headless bus does not supply native thread status, mid-turn steering or provider allowance routing.'],
  },
  {
    id: 'grok', number: '04', role: 'XAI / GROK', name: 'The routed specialist.',
    mark: './assets/providers/grok.svg', setup: './guide.html#providers',
    summary: 'A configured Grok client joins the collective through MCP or its optional conductor. See how prepared recall reaches the task, and where the result returns.',
    tags: ['Grok Build CLI', 'Optional conductor', 'Prepared recall'],
    parts: [
      { key: 'cortex', anatomy: 'Recall cortex', name: 'Context before dispatch.', status: 'OPTIONAL / DEFAULT OFF',
        description: 'A configured memory hook can prepare bounded recall for a Grok conductor run. The memoryHookPath starts unset. A separate recall-digest helper is bundled as source and requires owner setup and explicit scopes.',
        stack: ['Optional memoryHookPath', 'Scoped recall digest', 'Mem0'], source: source('conductor/providers/grok-conductor.mjs', 465) },
      { key: 'core', anatomy: 'Provider core', name: 'Your Grok session.', status: 'OWNER SETUP REQUIRED',
        description: 'Install and authenticate a compatible Grok Build CLI. The optional conductor starts disabled; once configured it supports prompt-file dispatch, new or resumed sessions, and interrupt/resume steering. Model selection belongs to your configured Grok runtime.',
        stack: ['Grok Build CLI', 'Optional conductor', 'New / resumed sessions'], source: source('docs/SETUP.md', 151) },
      { key: 'spine', anatomy: 'Memory spine', name: 'Delivery is not capture.', status: 'EXPLICIT FACT WRITES',
        description: 'The optional prepare/commit hook tracks whether recall was delivered after substantive output. That ledger is not a fact-capture system. Persist new knowledge with an explicit remember call or a separately configured capture path.',
        stack: ['Prepare recall', 'Delivery ledger', 'Explicit remember'], source: source('conductor/providers/grok-conductor.mjs', 465) },
      { key: 'tools', anatomy: 'Tool arm', name: 'Shared tools, your profile.', status: 'MCP SETUP',
        description: 'Register BORG in the Grok client’s MCP configuration to expose memory, files, commands, browser automation and configured remote targets. Each connection uses the owner’s access and declared target identities.',
        stack: ['MCP', 'Computer tools', 'Remote fleet'], source: source('docs/SETUP.md', 151) },
      memoryModels,
    ],
    flow: [
      { label: 'Provision profile', part: 'core', detail: 'Install a compatible Grok CLI, authenticate it and configure its own profile.' },
      { label: 'Choose runtime', part: 'core', detail: 'Use the native client or enable and configure the optional Grok conductor.' },
      { label: 'Prepare recall', part: 'cortex', detail: 'Request context through MCP, or configure the optional bounded recall hook before dispatch.' },
      { label: 'Run the task', part: 'tools', detail: 'The configured Grok runtime reasons and executes using the available tool connection.' },
      { label: 'Record delivery', part: 'spine', detail: 'An optional hook commit records recall delivery after output; inspect task receipts to verify the work.' },
      { label: 'Remember facts', part: 'models', detail: 'Explicitly write supported new facts into the shared memory stack. Recall delivery alone does not save them.' },
    ],
    boundaries: ['Grok does not receive Codex’s automatic hooks. Its optional conductor and memory hook need owner configuration.', 'A binary/version readiness check does not prove provider authentication or remaining allowance.'],
  },
  {
    id: 'tools', number: '05', role: 'BORG / TOOLS & SERVICES', name: 'The execution unit.',
    mark: './assets/mark.svg', setup: './guide.html#fleet',
    summary: 'The tool arm of the collective: files, commands, browsers, desktop control and explicitly registered remote machines. Follow a request from discovery to a verifiable result.',
    tags: ['Computer tools', 'Multiple remote targets', 'Operation receipts'],
    parts: [
      { key: 'cortex', anatomy: 'Discovery cortex', name: 'Find a capability.', status: 'CATALOG + REGISTRIES',
        description: 'Service discovery tells agents which configured capabilities they can call. The platform configurator lists possible integrations; a catalog choice becomes a working connection only after the owner installs and authenticates it.',
        stack: ['Service discovery', 'Integration catalog', 'Owner configuration'], source: source('connector/README.md', 15) },
      { key: 'core', anatomy: 'Routing core', name: 'One request. The right machine.', status: 'REGISTERED TARGETS',
        description: 'Fleet tools address explicitly registered machines and verify their identity. Independent work can run concurrently; operations that share a resource still use locks. Register each remote target before directing work to it.',
        stack: ['Target identities', 'Remote routing', 'Resource locks'], source: source('connector/fleet_tools.py', 151) },
      { key: 'spine', anatomy: 'Receipt spine', name: 'Trace work to its result.', status: 'OPERATION & JOB TOOLS',
        description: 'Operation status, recent-operation records and job tools make long-running work inspectable. If a caller loses its response, the operation may still be running: recover its receipt before submitting duplicate work.',
        stack: ['borg_operation_status', 'Recent operations', 'job_*'], source: source('connector/README.md', 21) },
      { key: 'tools', anatomy: 'Execution arm', name: 'Files. Processes. Browser. Desktop.', status: 'HOST PERMISSIONS APPLY',
        description: 'Run commands, edit files and control isolated browser sessions through native tools. Desktop control requires the host’s operating-system permissions. Browser profiles start fresh instead of importing your existing personal session.',
        stack: ['Files & processes', 'Isolated browser / CDP', 'macOS desktop'], source: source('connector/computer_tools.py', 339) },
      { key: 'models', anatomy: 'Integration bay', name: 'Connect your services.', status: 'OWNER AUTHENTICATION',
        description: 'Add the services your workflows need and store their credentials through the supported credential system. Agents discover metadata and handles; the registry does not return secret values. New installs start with empty owner registries.',
        stack: ['Service registry', 'Credential handles', 'Owner-installed integrations'], source: source('connector/README.md', 15) },
    ],
    flow: [
      { label: 'Discover tool', part: 'cortex', detail: 'Inspect the configured tool or service catalog to choose a capability that fits the task.' },
      { label: 'Select target', part: 'core', detail: 'Choose an explicit local or registered remote target and verify its identity.' },
      { label: 'Check access', part: 'models', detail: 'Use the owner’s configured connection, credential handle and required host permissions.' },
      { label: 'Execute work', part: 'tools', detail: 'Call a file, process, browser, desktop or service tool. Shared resources retain their coordination locks.' },
      { label: 'Read receipt', part: 'spine', detail: 'Inspect operation or job status, especially if the response was interrupted.' },
      { label: 'Verify result', part: 'tools', detail: 'Check the actual artifact or native service state before treating the work as complete.' },
    ],
    boundaries: ['Integration choices are configuration options, not preconnected accounts or automatically deployed services.', 'A tools-only installation can run this layer without local memory models. Remote targets and desktop permissions still require setup.'],
  },
];
